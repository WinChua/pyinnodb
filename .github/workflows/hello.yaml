name: hello
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: |
      curl -sSf https://rye-up.com/get > install.sh
      RYE_INSTALL_OPTION=-y bash install.sh
      source ~/.rye/env
      ls
